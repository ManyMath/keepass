name: keepass
description: Dart SDK for KeePass KDBX databases powered by Rust
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  ffigen: ^14.0.1
  test: ^1.25.0
